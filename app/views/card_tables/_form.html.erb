<%= form_with(model: card_table, local: true) do |form| %>
  <% if card_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card_table.errors.count, "error") %> prohibited this card_table from being saved:</h2>

      <ul>
      <% card_table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :column_index %>
    <%= form.text_field :column_index %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
